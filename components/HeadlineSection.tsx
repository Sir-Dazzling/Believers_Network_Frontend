import Link from "next/link";
import React, { useState } from "react";

export const HeadlineSection = () =>
{
    const [isOpen, setIsOpen] = useState(false);

    return (
        <div className="py-12">
            <div className="max-w-7xl mx-auto px-2 xs:px-2 sm:px-16 md:px-8 lg:px-1">
                yooo
            </div>
        </div>
    );
};